const message: str = "Hello, World!";
const ident msg_block = "message1";

::mindy::print(message, "\n");

let cond: bool = false;
let cond2 = false;
let test1: bool = cond;

if (cond) {
    ::mindy::flush_to(msg_block);
}

loop {}
