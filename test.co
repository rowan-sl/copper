// Hello, World!

// const msg: str = "Hello, World!";
const ident out = "message2";

fn main() -> num {
    // let a: num = 10;
    // let x: num = fib(4);
    // let a: bool = 1 < 2;
    // let a: bool = 1 <= 2;
    // let a: bool = 1 > 2;
    // let a: bool = 1 >= 2;
    // let a: bool = 1 == 2;
    // let a: bool = 1 + 2;
    // let a: bool = 1 - 2;
    // let a: bool = 1 * 2;
    // let a: bool = 1 / 2;
    // let a: bool = 1 % 2;
    // let a: bool = true || false;
    // let a: bool = true && false;

    // let i: num = 0;
    // while i < 10 {
    //     i = i + 1;
    //     println(i);
    // }
    // printflush(out);

    // println(msg);
    // printflush(out);
    // println("Very veryyyyyyyyyyyyyyyyyyyyyyyyyyyyyy long textttttttttt");
    // printflush(out);
    // if true {
    //     let a: num = 1;
    // }
    hello();
    // println("Main fn done");
    // printflush(out);
    return 0;
}

fn hello() -> null {
    println("Hello, World!");
    printflush(out);
    return;
}

// fn fib(n: num) -> num {
//     if n <= 1 {
//         return n;
//     }

//     let grandparent: num = 1;
//     let parent: num = 2;
//     let me: num = parent;
//     let i: num = 3;
//     while i < n {
//         i = i + 1;
//         me = parent + grandparent;
//         grandparent = parent;
//         parent = me;
//     }

//     return me;
// }
